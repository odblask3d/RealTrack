<?xml version="1.0" ?>
<class>

    <namespace>NVL_App</namespace>
    <solution>RealTrackLib</solution>
    <name>MapMerger</name>
    <description>Contains the logic to merging multiple depth maps</description>

    <includes>
        <include name="iostream" namespace="std" local="false" />
        <include name="opencv2/opencv.hpp" namespace="cv" local="false" />
    </includes>

    <methods>

        <method section="Merge" modifiers="static" access="public" return="Mat" name="Merger" description="The logic for merging maps" inline="false">
            <parameter type="Mat&" name="map1" description="The first map that we are merging" />
            <parameter type="Mat&" name="map2" description="The second map that we are merging" />
            <parameter type="Mat&" name="counters" description="The counters for defining the merge weights" />
        </method>

    </methods>

</class>